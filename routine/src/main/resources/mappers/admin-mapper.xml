<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.spring.admin.model.dao.AdminDao">
    
    <!-- ========== 공지사항 관리 ========== -->
    
    <!-- 관리자용 - 전체 공지사항 조회 -->
    <select id="selectNoticeList" resultType="com.kh.spring.notice.model.vo.Notice">
        SELECT 
            NOTICE_NUM as noticeNum,
            NOTICE_TITLE as noticetitle,
            NOTICE as notice,
            CREATEDATE as createdate
        FROM BOARD
        ORDER BY NOTICE_NUM DESC
    </select>
    
    <!-- 관리자용 - 공지사항 상세 조회 -->
    <select id="selectNoticeByNum" parameterType="int" resultType="com.kh.spring.notice.model.vo.Notice">
        SELECT 
            NOTICE_NUM,
            NOTICE_TITLE,
            NOTICE,
            CREATEDATE
        FROM BOARD
        WHERE NOTICE_NUM = #{noticeNum}
    </select>
    
    <!-- 관리자용 - 공지사항 등록 -->
    <insert id="insertNotice" parameterType="com.kh.spring.notice.model.vo.Notice">
        INSERT INTO BOARD (NOTICE_NUM, NOTICE_TITLE, NOTICE)
        VALUES (BOARD_SEQ.NEXTVAL, #{noticetitle}, #{notice})
    </insert>
    
    <!-- 관리자용 - 공지사항 수정 -->
    <update id="updateNotice" parameterType="com.kh.spring.notice.model.vo.Notice">
        UPDATE BOARD
        SET NOTICE_TITLE = #{noticetitle},
            NOTICE = #{notice}
        WHERE NOTICE_NUM = #{noticeNum}
    </update>
    
    <!-- 관리자용 - 공지사항 삭제 -->
    <delete id="deleteNotice" parameterType="int">
        DELETE FROM BOARD
        WHERE NOTICE_NUM = #{noticeNum}
    </delete>
    
    <!-- 관리자용 - 전체 공지사항 개수 조회 -->
    <select id="selectNoticeCount" resultType="int">
        SELECT COUNT(*)
        FROM BOARD
    </select>
    
    <!-- 관리자용 - 최근 공지사항 조회 -->
    <select id="selectRecentNotices" parameterType="int" resultType="com.kh.spring.notice.model.vo.Notice">
        <![CDATA[
        SELECT * FROM (
            SELECT 
                NOTICE_NUM,
                NOTICE_TITLE,
                NOTICE,
                CREATEDATE
            FROM BOARD
            ORDER BY CREATEDATE DESC
        ) WHERE ROWNUM <= #{limit}
        ]]>
    </select>
    
    <!--  -->

    <!-- 회원 상세 조회 -->
    <select id="selectUserByNo" parameterType="int" resultType="User">
        SELECT 
            USER_NO as userNo,
            USER_ID as userId,
            NAME as name,
            NICK as nick,
            EMAIL as email,
            ROLE as role,
            CREATED_AT as createdAt,
            STATUS as status
        FROM USERS
        WHERE USER_NO = #{userNo}
    </select>

    <!-- 전체 회원 목록 조회 -->
    <select id="selectUserList" resultType="User">
        SELECT 
            USER_NO as userNo,
            USER_ID as userId,
            NAME as name,
            NICK as nick,
            EMAIL as email,
            ROLE as role,
            CREATED_AT as createdAt,
            STATUS as status
        FROM USERS
        ORDER BY CREATED_AT DESC
    </select>

    <!-- 전체 회원 수 -->
    <select id="selectUserCount" resultType="int">
        SELECT COUNT(*)
        FROM USERS
        WHERE STATUS = 'Y'
    </select>

    <!-- 활성 회원 수 -->
    <select id="selectActiveUserCount" resultType="int">
        SELECT COUNT(*)
        FROM USERS
        WHERE STATUS = 'Y'
    </select>

    <!-- 비활성 회원 수 -->
    <select id="selectInactiveUserCount" resultType="int">
        SELECT COUNT(*)
        FROM USERS
        WHERE STATUS = 'N'
    </select>

    <!-- 관리자 수 -->
    <select id="selectAdminCount" resultType="int">
        SELECT COUNT(*)
        FROM USERS
        WHERE ROLE = 'ADMIN' AND STATUS = 'Y'
    </select>

    <!-- 일반 회원 수 -->
    <select id="selectGeneralUserCount" resultType="int">
        SELECT COUNT(*)
        FROM USERS
        WHERE ROLE = 'USER' AND STATUS = 'Y'
    </select>

    <!-- 최근 가입 회원 조회 -->
    <select id="selectRecentUsers" parameterType="int" resultType="User">
        <![CDATA[
        SELECT * FROM (
            SELECT 
                USER_NO as userNo,
                USER_ID as userId,
                NAME as name,
                NICK as nick,
                EMAIL as email,
                ROLE as role,
                CREATED_AT as createdAt,
                STATUS as status
            FROM USERS
            WHERE STATUS = 'Y'
            ORDER BY CREATED_AT DESC
        ) WHERE ROWNUM <= #{limit}
        ]]>
    </select>
    
</mapper>